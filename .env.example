# Database (Railway provides DATABASE_URL when you add PostgreSQL)
DATABASE_URL="postgresql://user:password@localhost:5432/ai_community_platform?schema=public"

# Auth (NextAuth)
AUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# API v1 (Creator Publishing) - JWT for REST API
JWT_SECRET="generate-with-openssl-rand-base64-32"

# OAuth (optional - for social login)
# AUTH_GOOGLE_ID=""
# AUTH_GOOGLE_SECRET=""
# AUTH_GITHUB_ID=""
# AUTH_GITHUB_SECRET=""

# OpenAI (for AI features)
OPENAI_API_KEY="sk-..."

# Stripe (for payments)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# Resend (for contact form emails - all submissions go to info@francescatabor.com)
RESEND_API_KEY="re_..."
RESEND_FROM_EMAIL="AI Community Platform <onboarding@resend.dev>"

# Email infrastructure (queue, worker, webhooks)
# NEXT_PUBLIC_APP_URL="https://your-domain.com"  # For unsubscribe links and post URLs
# CRON_SECRET="..."  # Required for /api/email/worker cron trigger
# EMAIL_INTERNAL_API_KEY="..."  # Optional: for /api/email/send internal API auth

# Admin (required for admin dashboard at /admin)
ADMIN_SECRET="generate-a-secure-random-string"

# Storage (optional - for S3/R2)
# AWS_ACCESS_KEY_ID=""
# AWS_SECRET_ACCESS_KEY=""
# AWS_S3_BUCKET=""
# AWS_REGION=""
